const { expect } = require('chai');

it('should have a description here', function() {
    // Code to test
});

it('should add numbers correctly', function() {
    const num1 = 2;
    const num2 = 3;

    expect(num1 + num2).to.equals(5);
})